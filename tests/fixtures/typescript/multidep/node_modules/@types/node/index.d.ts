// Minimal stand-in for @types/node: just enough surface for the fixture,
// resolved through a real node_modules lookup like the full package would be.
declare module "node:fs" {
    function readFileSync(path: string, encoding: string): string;
    export { readFileSync };
}
